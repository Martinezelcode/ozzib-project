{
  "name": "bantah-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot for Bantah - runs independently and calls main API",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js --external:axios",
    "start": "NODE_ENV=production node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["telegram", "bot"],
  "author": "Bantah",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "node-telegram-bot-api": "^0.64.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.27.1",
    "tsx": "^4.7.0",
    "typescript": "^5.9.0"
  }
}
